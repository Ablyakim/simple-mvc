<div class="form-group">
    <label for="username">Username</label>
    <input type="text" name="username" value="{{ task.username }}" class="form-control" id="username" required
           placeholder="Username">
</div>

<div class="form-group">
    <label for="username">Email</label>
    <input type="email" name="email" value="{{ task.email }}" class="form-control" id="username" required
           placeholder="Email">
</div>
{% if auth_manager.loggedIn %}
    <div class="checkbox">
        <label>
            <input type="checkbox" {% if task.status %}checked{% endif %} name="status" value="1"> Status
        </label>
    </div>
{% endif %}

<div class="form-group">
    <label for="content">Content</label>
    <textarea name="content" class="form-control" id="content">{{ task.content }}</textarea>
</div>


<button class="btn-success btn" type="submit">Save</button>